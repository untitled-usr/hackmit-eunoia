## Mid-auth service origin.
## Keep frontend and mid-auth on the same host (both localhost or both 127.0.0.1)
## so browser can consistently send session cookies.
VITE_MID_AUTH_ORIGIN=http://127.0.0.1:19000
